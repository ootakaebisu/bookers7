<div class = "container">
  <div class="row">
    <div class="col-xs-3">
      <%= render 'users/user', user: @user %>
      <%= render 'books/newbook', book: @book %>
    </div>
  
    <div class="col-xs-9">
      <h2>Users</h2>
  
      <!--<table>-->
      <!--  <tr>-->
      <!--    <th>image</th>-->
      <!--    <th>name</th>-->
      <!--    <th></th>-->
      <!--  </tr>-->
      <!--  < % @users.each do |user| %>-->
      <!--    <tr>-->
      <!--      <td>< %= attachment_image_tag user, :profile_image, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: "no_image.jpg", size: "40x40" %></td>-->
      <!--      <td>< %= user.name %></td>-->
      <!--      <td>< %= link_to "show", user_path(user.id) %></td>-->
      <!--    </tr>-->
      <!--  < % end %>-->
      <!--</table>-->
  
      <table class="table">
      <thead>
        <tr>
          <th scope="col">image</th>
          <th scope="col">name</th>
          <th scope="col"></th>
        </tr>
      </thead>
      <% @users.each do |user| %>
      <tbody>
        <tr>
          <th scope="row"><%= attachment_image_tag user, :profile_image, format: 'jpeg', class: "img-circle pull-left profile-img", fallback: "no_image.jpg", size: "40x40" %></th>
          <td><%= user.name %></td>
          <td><%= link_to "Show", user_path(user.id) %></td>
        </tr>
      </tbody>
      <% end %>
      </thead>
  
    </div>
  
  </div>
</div>

